list_one = ["A",2,5,6,2]
list_two = [1,2,5,6,2]


def compate_lists(list_one, list_two):
	if list_one == list_two:
		print "The lists are the same"
	if list_one != list_two:
		print "The lists are not the same."

compate_lists(list_one, list_two)